type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  owner: Bytes! # address
  approved: Bytes! # address
}

type vNFT @entity {
  id: ID!
  score: BigInt!
  #owner:
  #level:
  #isAlive:
  #expectedReward:
  #timeUntilStarving:
  #lastTimeMined:
  #timeVnftBorn:
  #fatalityReward:
}
