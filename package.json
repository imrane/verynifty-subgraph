{
  "name": "verynifty-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ imrane/verynifty",
    "create-local": "graph create --node http://localhost:8020/ imrane/verynifty",
    "remove-local": "graph remove --node http://localhost:8020/ imrane/verynifty",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 imrane/verynifty"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
